# Changelog

Format: `YYYY-MM-DD · <service> · <feature> — one-line summary`

Append only. Never rewrite or reorder. Per-service section ownership:
- `pipeline` — Session A
- `api` — Session B
- `web` — Session C
- `contract` — Session 0 (schema, OpenAPI, infra, docs)

---

## 2026-05-27 — Phase 1 (in progress)

- **2026-05-27 · contract · session-0-bootstrap** — Monorepo scaffold: root README, CHANGELOG, CLAUDE.md, AGENTS.md, .gitignore, .env.example. Service directories created as empty.
- **2026-05-27 · contract · postgres-schema** — Initial schema in `supabase/migrations/0001_initial_schema.sql`: products, store_listings, price_history (monthly range partitions for 2026-05/06/07), reviews, review_chunks + spec_chunks (btree on product_id, no HNSW per ADR-001), ai_summaries, stores. pgvector + pg_trgm + pgcrypto extensions enabled. Seeded 8 stores.
- **2026-05-27 · contract · docker-compose** — Postgres 16+pgvector with auto-applied migrations, Redis 7-alpine with healthchecks, profiled service stubs for pipeline/api/web.
- **2026-05-27 · contract · openapi-spec** — `docs/api/openapi.yaml` covering all Phase 1 endpoints (search, detail, prices, reviews, summary, chat SSE).
- **2026-05-27 · contract · service-claude-md** — Per-service CLAUDE.md scope files for pipeline, api, web (SPEC §19.6).
- **2026-05-27 · contract · adr-stubs** — ADR-001 through ADR-012 transcribed from SPEC §18; ADR-013 through ADR-020 reserved as empty stubs (Pipeline 013-015, API 016-018, Web 019-020).
- **2026-05-27 · contract · claude-pr-review** — GitHub Actions workflow `.github/workflows/claude-pr-review.yml` invokes `anthropics/claude-code-action@v1` on pull_request open/sync/reopen. Prompt enforces scope boundaries, OpenAPI/schema contract conformance, ADR compliance (1-12), and CHANGELOG discipline. Auto-merge intentionally not enabled — manual merge after review.
- **2026-05-27 · contract · claude-pr-review-auth-swap** — Switched workflow auth from `anthropic_api_key` to `claude_code_oauth_token` to use Claude Max subscription quota instead of API credits. Model dropped to `claude-haiku-4-5`. Requires Claude Code GitHub App installed on the repo (https://github.com/apps/claude).
- **2026-05-27 · contract · claude-pr-review-opus** — Upgraded model to `claude-opus-4-7` for review (Max quota covers it). Workflow comment documents Haiku as the model to use if reverting to API key auth.
- **2026-05-27 · contract · claude-pr-review-sticky-comment** — Fix: agent mode wasn't posting reviews. Added `use_sticky_comment: "true"` + `track_progress: "false"` so Claude publishes the verdict as a single consolidated comment. Capped `--max-turns 8` and tightened prompt to ~50% original token count to cap per-PR cost (prior run: 25 turns, $1.20 in Max-equivalent compute, no posted comment).
- **2026-05-27 · contract · claude-pr-review-turn-budget** — Bumped `--max-turns` from 8 to 20 after pipeline PR (large Rust diff) hit `Reached maximum number of turns (8)`. Added explicit turn-budget allocation in prompt instructing Claude to prioritize BLOCKING checks over code-quality deep-reads and ship the verdict by turn 19.
- **2026-05-27 · contract · claude-pr-review-uncap** — Removed `--max-turns` cap entirely and stripped the turn-budget block from the prompt. On Claude Max subscription, quota throttling is the only ceiling — revisit caps only if Max limits are hit during heavy multi-session days.
- **2026-05-27 · contract · claude-pr-review-allowed-tools** — Root cause for "no posted comment": `--allowedTools` was unset, so Claude's `gh pr comment` calls were sandboxed (6 permission denials per run). Added explicit allowlist for `gh pr comment/diff/view/files`, git read commands, and Read/Glob/Grep. Prompt now explicitly instructs Claude to post via `gh pr comment` and to NOT submit the review as its final agent message.
- **2026-05-27 · pipeline · cargo-workspace** — `Cargo.toml` + `rust-toolchain.toml` (1.83 stable) pinning tokio/sqlx/reqwest/redis with rustls; pedantic clippy + forbid unsafe.
- **2026-05-27 · pipeline · db-pool-migrations** — sqlx Postgres pool (max 8) + lightweight runtime migration runner with SHA-256 checksum drift detection, reading `supabase/migrations/`.
- **2026-05-27 · pipeline · amazon-paapi-scraper** — `StoreCrawler` trait + Amazon PA-API 5.0 `GetItems` client with AWS SigV4 signing and 10-ASIN auto-chunking; raw payloads archived to `_gn_scrape_audit`.
- **2026-05-27 · pipeline · normalizer** — title/brand/category canonicalization: ~40-brand alias table, digit-bearing model-token regex, sentence-aware spec chunker, breadcrumb→canonical category projector.
- **2026-05-27 · pipeline · entity-resolution-3tier** — Tier 1 GTIN/ASIN identity, Tier 2 `<brand>:<model>` canonical key, Tier 3 cosine-similarity skeleton with 0.92/0.80 confidence cutoffs (ADR-007).
- **2026-05-27 · pipeline · embeddings** — HuggingFace Inference API client (bge-small-en-v1.5, 384d) with 32-input auto-batching + multi-row pgvector bulk insert; sentence-boundary spec chunks and 256/32 fixed-overlap review chunks.
- **2026-05-27 · pipeline · redis-price-swr** — stale-while-revalidate Redis hash writer with first-write 0–60 min jitter persisted in payload (ADR-009); no key TTL.
- **2026-05-27 · pipeline · price-history-partitions** — idempotent monthly partition DDL at startup (current ±1, +2), batched append, and latest-per-listing fallback path (ADR-010).
- **2026-05-27 · pipeline · integration-test-amazon-50** — end-to-end wiremock-driven test scraping 50 PA-API fixtures through normalize → resolve → embed → DB, asserting audit/listing/product/spec-chunk counts.
- **2026-05-27 · api · spring-boot-scaffold** — `pom.xml` (Spring Boot 3.3.5, Java 21), `application.yml` with `spring.threads.virtual.enabled=true`, Maven wrapper, Dockerfile, package layout under `io.gearnest.api` (config, product, search, pricing, rag, session, embedding, error).
- **2026-05-27 · api · product-controller** — `GET /api/v1/products/search` and `GET /api/v1/products/{slug}` matching `docs/api/openapi.yaml`; DTO records mirror schema component names.
- **2026-05-27 · api · search-service** — `SearchService` + `ProductRepository.hybridSearch` combining pgvector cosine (`<=>`) at 0.6 weight with PostgreSQL FTS `ts_rank` at 0.4 weight, per ADR-005 / SPEC §8.5.
- **2026-05-27 · api · pricing-service** — `PricingService` reads `price:listing:{id}` Redis hash for live price/stock, falls back to latest `price_history` row, sets `isStale=true` when fallback is used or Redis snapshot is older than 25h (ADR-009).
- **2026-05-27 · api · best-value-scorer** — `BestValueScorer` ranks listings by `0.4×price_score + 0.6×rating_score` with 0.02 tiebreaker by price ascending; marks top listing `isBestValue=true`; out-of-stock sorted last (SPEC §12).
- **2026-05-27 · api · session-budget-service** — Redis WATCH/MULTI reserve-then-commit (`DECR questions_remaining` + `SET inflight EX 90`), `commit()` clears inflight on first token, `rollback()` restores budget on HF first-token timeout (SPEC §14).
- **2026-05-27 · api · rag-controller** — `GET /api/v1/chat` SSE endpoint; `gn_session` cookie issued if absent; streaming runs on `Thread.ofVirtual()`; events `token` / `done` / `limit_reached` / `error` match OpenAPI.
- **2026-05-27 · api · rag-service** — Stratified retrieval (5 MMR + 3 negative + 2 spec) over product-scoped pgvector KNN, MMR λ=0.7, HuggingFace chat client with 30s first-token timeout → budget rollback (SPEC §11 + ADR-008).
- **2026-05-27 · api · chat-integration-test** — `ChatStreamIntegrationTest` seeds a product + spec/review chunks in Testcontainers `pgvector/pgvector:pg16`, hits `GET /api/v1/chat`, asserts SSE body contains `event:token`, `event:done`, and the seeded product name.
- **2026-05-27 · web · next16-scaffold** — Next.js 16 + React 19 + TypeScript strict + `@/` alias. Tailwind v4 via `@tailwindcss/postcss` with `@theme` tokens and `@custom-variant dark` in `app/globals.css`. Flat ESLint config consuming `eslint-config-next` + core-web-vitals.
- **2026-05-27 · web · mock-data** — `lib/api/types.ts` mirrors the OpenAPI schema; `lib/mock/products.ts` ships 6 typed products spanning camping, apparel, footwear, electronics with stratified review tiers and per-store listings; `lib/api/client.ts` swaps between mocks and real API via `NEXT_PUBLIC_API_BASE`.
- **2026-05-27 · web · catalog-page** — `app/(catalog)/page.tsx` Server Component renders filterable product grid with `FacetSidebar` (sort, category, brand, price range, min rating) driven by awaited `searchParams`.
- **2026-05-27 · web · product-detail** — `app/(catalog)/[slug]/page.tsx` Server Component composes specs, `PriceTable`, `ReviewTierList`, and `ChatPanel`; async `params`/`searchParams` per Next 16.
- **2026-05-27 · web · search-bar** — `components/search/SearchBar.tsx` debounced (220ms) client component backed by `searchSuggestions` Server Action; submits navigate to `/?q=`.
- **2026-05-27 · web · chat-panel** — `components/chat/ChatPanel.tsx` SSE consumer over `EventSource` (token/done/limit_reached/error events), 5-question session-budget indicator, mock token streamer until API merges.
- **2026-05-27 · web · price-table** — `components/prices/PriceTable.tsx` per-store rows sorted by best-value score, stale badge (>24h), best-value highlight, CANDIDATE listings filtered out per ADR-007.
- **2026-05-27 · web · api-integration** — `lib/api/client.ts` flipped to call the live Spring API at `NEXT_PUBLIC_API_BASE` (default `http://localhost:8080`); mocks now opt-in via `NEXT_PUBLIC_USE_MOCKS=1`. Network/404 errors resolved to `null` so detail page degrades gracefully when reviews endpoint (Phase 3) is absent. `.env.local.example` documents the toggles.
- **2026-05-27 · contract · redis-schema-contract** — Pinned the Redis schema in `docs/contracts/redis-schema.md` (price cache, session budget, last-updated marker) as the single source of truth. Root-caused the price divergence to a Session 0 gap — the Redis interface was prose, not a reviewable artifact. ADR-0021 (contract-owned) records the decision.
- **2026-05-27 · api · fix-dockerfile-unzip** — Integration build failed: `eclipse-temurin:21-jdk` lacks `unzip`, so `./mvnw` couldn't extract Maven (exit 127). Added `unzip` install to the Dockerfile build stage.
- **2026-05-27 · api · fix-mvnw-noglob** — `mvnw` ran under `set -euf`; the `-f` (noglob) disabled pathname expansion, so the `mv apache-maven-*/*` extraction step failed with "cannot stat". Re-enabled globbing inside the extraction subshell (`set +f`). Affected Docker build, CI `test.yml`, and fresh local checkouts.
- **2026-05-27 · api · fix-redis-price-schema** — Integration bug: API read `price:listing:{id}` (flat fields) but the pipeline writes `prices:{product_id}` field=`store_id` JSON. All prices were silently stale (Redis always missed → price_history fallback). Fixed `PricingService.readSnapshot` to `(productId, storeId)` + JSON parse, matching the pipeline + SPEC §6-7 (ADR-006/009). Added `PricingRedisContractTest` regression test. Schema pinned in `docs/contracts/redis-schema.md` (separate contract PR); ADR-0021 (contract-owned) records the decision.
- **2026-05-27 · api · fix-price-staleness-jitter** — `PricingService` now reads the payload's `jitter_secs` and computes staleness as `24h + jitter_secs`, matching the pipeline's `PricePayload::is_stale` exactly (was a flat 25h ignoring jitter). Narrowed the snapshot-parse `catch` to `JsonProcessingException | NumberFormatException` so genuine read errors aren't all swallowed.
- **2026-05-27 · contract · ci-test-workflow** — Added `.github/workflows/test.yml` (SPEC §15.6) — the missing CI gate that let the integration bugs through. Path-filtered jobs (dorny/paths-filter): pipeline (`cargo fmt`/`clippy`/`test` + `migrate` + `--ignored` e2e against pgvector+redis service containers), api (`./mvnw test` via Testcontainers), web (`npm ci`/`typecheck`/`lint`/`build`). Now `PricingRedisContractTest` + all suites actually run on every PR.
- **2026-05-27 · web · app-icon** — Added `app/icon.svg` (Next.js App Router file convention → auto-served favicon + injected `<link>`). Minimalist GearNest mark per SPEC §2: white mountain range + snowcap notch + cradling "nest" baseline arc on brand green (`#2f7a3a`). Replaces the default Next.js favicon.
- **2026-05-28 · api · hf-router-migration** — Migrated HF clients off the retired `api-inference.huggingface.co` to `router.huggingface.co`. Chat client → OpenAI-compatible `/v1/chat/completions` (parses `choices[].delta.content`, stops on `[DONE]`); embedding client → `/hf-inference/models/{model}/pipeline/feature-extraction`. Default chat model `Qwen/Qwen2.5-7B-Instruct` (Mistral-7B-Instruct-v0.3 is rejected as "not a chat model" on the router). `RagService` now logs HF errors + response body instead of silently swallowing them. Verified: chat streams real grounded tokens end-to-end.
- **2026-05-28 · contract · local-dev-env-wiring** — `docker-compose.yml` loads `.env.local` into the `api` + `pipeline` containers (`env_file`, optional) and mounts `supabase/migrations` into the pipeline container so `migrate` works. `.env.example` corrected to the variable names the code actually reads (`PAAPI_*` not `AMAZON_PA_*`, `NEXT_PUBLIC_API_BASE` not `_URL`), adds `HUGGINGFACE_BASE_URL` (read by the pipeline), HF router base URL + Qwen chat model defaults, annotated per service. Fixes the env-var drift that broke local secret loading.
- **2026-05-28 · pipeline · hf-router-migration** — HF retired `api-inference.huggingface.co`; embeddings now POST `router.huggingface.co/hf-inference/models/{model}/pipeline/feature-extraction`. Reads `HUGGINGFACE_API_KEY` (was `HUGGINGFACE_TOKEN`) for consistency with the API + `.env.example`. New multi-stage `Dockerfile` (rust:1.83-slim → debian-slim) so `docker compose --profile pipeline` builds and runs.
- **2026-05-28 · pipeline · cargo-fmt** — Ran `cargo fmt` across the crate. The code merged (PR #2) before the `test.yml` fmt gate existed (PR #6); this is the first pipeline PR to run `cargo fmt --check`, which surfaced the pre-existing formatting debt. Formatting-only, no behavior change.
- **2026-05-28 · pipeline · toolchain-1.95-lockfile** — Bumped `rust-toolchain.toml` + `Dockerfile` to Rust 1.95.0 and committed `Cargo.lock`. The pinned 1.83 was too old for current deps (the `wiremock` dev-dep needs `edition2024`/Rust ≥1.85), and the missing lockfile let unpinned deps drift on every build; builds are now reproducible.
- **2026-05-28 · pipeline · fix-compile-and-clippy** — Fixed a real compile error that shipped in PR #2 undetected (no CI compiled it then): `normalizer/mod.rs` called `.is_empty()` on an `Option<String>` → `.is_none()`. Brought the crate to `clippy --all-targets -D warnings` clean under 1.95: `write!` instead of `push_str(&format!)`, `i16::try_from` for chunk-index casts, by-reference `item_to_raw`, `Map::new()`, and scoped/commented `#[allow]`s for provably-safe month-math casts and test-only pedantic lints (production stays strict).
- **2026-05-28 · contract · phase2-plan** — Added `docs/PHASE2.md`: multi-store + price-comparison plan (7 PR breakdown, transport tiers per SPEC §7, `StoreCrawler` trait extension, live-scraping-runtime + captured-HTML-parser-test strategy, ADR-013/014/015 to be claimed). The reference brief for the Phase-2 pipeline session.
- **2026-05-28 · pipeline · storecrawler-transport-tiers** — Extended the `StoreCrawler` trait with `crawl_products(&Category)` (discovery — scrape stores have no ID list) and `fetch_price(&store_product_id)` as defaulted methods (kept `fetch_batch` for API stores; a mis-dispatch bails loudly). Added a `Tier`-selected `Transport` abstraction: clean-HTTP and proxy tiers share one browser-headers + cookie-jar `reqwest` client with an optional `SCRAPE_PROXY_{STORE}` proxy; headless (`chromiumoxide`) deferred to PR4. Normalizer/resolver/embedder untouched — a tier swap is a one-file change (SPEC §7, ADR-013).
- **2026-05-28 · pipeline · campsaver-scraper** — `CampSaver` reference clean-HTTP scraper: parses product fields from schema.org JSON-LD (`Product` + `BreadcrumbList`) via `parse_product` / `parse_price` / `parse_listing_urls`, with new `Category` / `PriceUpdate` models. Deterministic offline parser test (`tests/campsaver_parser.rs`) against a captured `tests/fixtures/campsaver_product.html` — the live site is hit only at runtime. Enabled the reqwest `cookies` feature for the clean-HTTP cookie jar.
- **2026-05-28 · pipeline · jsonld-shared-parser** — Extracted the schema.org JSON-LD parsing into a shared `scrapers::jsonld` module (parameterized by `store_id`), now that a second clean-HTTP store exists; `CampSaver` is a thin transport+URL adapter delegating to it (tests unchanged, behavior identical). Added offer-level `sku`/`gtin` lookup (Shopify puts them on the variant `Offer`, not the `Product`) and order-preserving dedup of discovered listing URLs.
- **2026-05-28 · pipeline · garagegrowngear-scraper** — Garage Grown Gear clean-HTTP scraper (Shopify `/collections` + `/products` URL scheme) over the shared JSON-LD parser. Deterministic offline parser test (`tests/garagegrowngear_parser.rs`) against a captured `tests/fixtures/garagegrowngear_product.html` exercising Shopify-shaped JSON-LD: a variant-`Offer` `sku`, a plain-string `brand`, and a no-GTIN cottage product (Tier-2 resolution territory). Store id is the migration-seeded slug `garagerowngear`.
- **2026-05-28 · pipeline · proxy-tier-stores** — Backcountry, Moosejaw, and Steep & Cheap scrapers on the `Tier::Proxy` transport (residential proxy via `SCRAPE_PROXY_{STORE}`, direct-HTTP fallback when unset). All three are Salesforce Commerce Cloud sites emitting schema.org JSON-LD, so they reuse the shared `scrapers::jsonld` parser unchanged — only the transport differs (ADR-014: rotation delegated to the proxy provider, not managed in-process). Captured-HTML fixtures + offline parser tests per store cover an `offers` array + `ratingCount` alias (Moosejaw), a GTIN for Tier-1 resolution (Backcountry), and flash-sale `LimitedAvailability` + no `aggregateRating` (Steep & Cheap).
- **2026-05-28 · pipeline · availability-not-unavailable** — `jsonld::availability_in_stock` now treats any schema.org availability that is not explicitly `OutOfStock` / `SoldOut` / `Discontinued` as in stock (was: only `InStock`), so `LimitedAvailability` and `PreOrder` count as purchasable. No change for the existing `InStock` stores.
- **2026-05-28 · pipeline · cabelas-headless** — Implemented the `Tier::Headless` transport with `chromiumoxide` (0.7, tokio-runtime, no default async-std): one lazily-launched browser + a `Semaphore`-capped 3-tab pool that renders a page and snapshots the DOM. Cabela's scraper rides it and delegates to the shared `jsonld` parser (rendered DOM still carries JSON-LD). Offline parser test against a captured-after-render `tests/fixtures/cabelas_product.html`; the live browser pool needs a Chrome binary and runs only at runtime (ADR-015).
- **2026-05-28 · pipeline · rei-cj-affiliate** — REI scraper on the API+scrape tier: a CJ (Commission Junction) GraphQL Product Search client (`CjConfig` reads `CJ_API_KEY` / `CJ_WEBSITE_ID` / `CJ_REI_ADVERTISER_ID` / `CJ_API_URL`) discovers REI's catalog, then a clean-HTTP scrape of each product page (shared `jsonld` parser) fills the editorial fields CJ omits. `merge_supplement` keeps CJ authoritative for price/stock/GTIN/id and lets the scrape fill blanks only (ADR-022). Deterministic tests: `parse_cj_response` against a captured `tests/fixtures/cj_rei_products.json`, a wiremock-stubbed `CjClient::search` (asserts Bearer auth + sale-price precedence), and the merge precedence — no live CJ creds or network.
- **2026-05-28 · pipeline · rei-adr-scope-fix** — Backed out `docs/adr/0022` + its `docs/adr/README.md` index entry that the previous REI PR added: ADR 021+ is the Contract/Session-0 range, not pipeline's (013–015). The CJ-primary / scrape-fills-blanks decision now lives in the `rei::merge_supplement` doc comment and is routed to Session 0 to record as a contract-owned ADR. Dropped the now-dangling `ADR-022` citations and aligned `cj_in_stock` with `jsonld::availability_in_stock` (treats `discontinued` as out of stock too).
- **2026-05-29 · contract · scheduling-adr** — ADR-0022: pipeline scheduling is external (Cloud Scheduler → one-shot Cloud Run Job), not in-process `tokio-cron-scheduler`. Updated `docs/PHASE2.md` PR 6 to build a one-shot `price-sync`/`full-sync` subcommand. Cheaper (ephemeral Jobs vs always-on container) under the $30/mo cap (ADR-012).
- **2026-05-28 · pipeline · price-sync** — Daily price-sync across all 8 stores (`price_sync::run`): for every active `store_listing` (EXACT/HIGH/MEDIUM only — CANDIDATE excluded per ADR-007) it calls the store's `fetch_price`, writes the Redis SWR hash (`PriceWriter`) + batches into `price_history` (`append_many`), then stamps `prices:last_updated` (RFC3339, per the Redis contract). Per-store `governor` rate limits (SPEC §7 table) throttle each store; transient 429/503/timeout errors retry with exponential backoff. Added `AmazonScraper::fetch_price` (single-ASIN `GetItems`) so the trait is uniform across tiers, and `PriceWriter::set_last_updated`. New one-shot CLI: `price-sync` (runs once and exits); scheduling is external (Cloud Scheduler → one-shot Cloud Run Job) per ADR-0022, wired in Phase 5. Deterministic tests: rate-limit coverage + transient-error classification (unit) and a DB+Redis `--ignored` e2e injecting a fake crawler to assert the CANDIDATE skip + Redis/`price_history` writes.
- **2026-05-29 · contract · rei-merge-adr** — ADR-0023: records the REI dual-source merge precedence (CJ affiliate authoritative for price/stock/GTIN/id/url/title; scrape fills only the editorial blanks). Formalizes the decision routed from PR #17 (which backed out a pipeline-misallocated 0022). Why: CJ is the paid-on click destination, so it stays canonical for the money/link.
- **2026-05-29 · pipeline · rei-gtin-cj-only** — Tightened `rei::merge_supplement` to conform to ADR-0023: GTIN is now CJ-only (`primary.gtin`) instead of falling back to a scraped GTIN when CJ omits one. A scraped GTIN could mis-drive Tier-1 entity resolution (ADR-007); without it, REI correctly falls back to Tier-2 attribute matching. Added a regression test.
- **2026-05-29 · contract · phase2-smoke-test** — `scripts/phase2_smoke.sh`: manual live integration smoke test, the executable definition of "Phase 2 done" (multi-store ingest → cross-store resolution per ADR-007 → 8-store Redis price comparison → API `/prices`). Not a CI gate (hits real retailer sites; CI keeps gating on the deterministic captured-HTML fixtures). Running it surfaced that no runtime path calls `crawl_products`: `scrape-amazon` is the only path that creates listings and `price-sync` only refreshes existing ones, so cross-store resolution never runs on real data. The harness fails loud at Stage 4 with that diagnosis until a one-shot `full-sync`/`crawl` subcommand lands (pipeline scope; docs/PHASE2.md PR 6 / SPEC §7).
- **2026-05-29 · api · candidate-recovery-loop** — `/api/admin/candidates` (list), `/{id}/confirm` (sync UPDATE → MEDIUM), `/{id}/reassign` (202 + jobId, virtual-thread `@Async` worker re-points `review_chunks` via the `reviews(product_id=old, store_id=listing.store_id)` join, flips the listing to HIGH, and clears `ai_summaries` for both old + new product). SPEC §7 Case A / Case B. In-memory job map (`CandidateService.jobFuture`) plumbs a future for a later polling endpoint — `/api/admin/jobs/{id}/status` is not yet in `openapi.yaml`; routed back to Session 0. Testcontainers coverage for both endpoints.
- **2026-05-29 · pipeline · full-sync-subcommand** — New one-shot `full-sync` CLI (`full_sync::run`) closes the crawl→persist seam phase2_smoke Stage 4 was failing on: for every crawler with a non-empty `StoreCrawler::categories()`, walks each category → `crawl_products` → drives each raw through the same pipeline `scrape-amazon` runs (record_raw → normalize → resolve → embed → upsert listing → Redis SWR + `price_history`), reusing `price_sync`'s per-store `governor` rate limits (SPEC §7). Per-store failures logged & skipped. Added a `categories()` trait method on `StoreCrawler` (default empty — Amazon stays ASIN-driven via `scrape-amazon`); the 7 scrape/CJ stores ship a curated 5-slug seed list each (URL-style for HTTP/proxy/headless tiers, CJ keywords for REI). Scheduled externally per ADR-0022 (`0 2 * * 0` weekly, wired in Phase 5). Deterministic `--ignored` e2e injects two fake crawlers under different store_ids whose Tier-2 `<brand>:<model>` collapses to one canonical product, asserting one product / two store_listings / both EXACT|HIGH|MEDIUM. Also added `gear-nest-pipeline/.dockerignore` (target/, .git/) — the previous Docker build context shipped ~2.3GB target/ each build and filled the Colima VM.
- **2026-05-29 · contract · smoke-test-fullsync-fix** — `scripts/phase2_smoke.sh` fixed against current main: (1) Stage 4 invokes `pipeline full-sync` without `--store` (matches the no-arg clap variant from #25); full-sync swallows per-store failures so missing proxy creds skip cleanly. (2) New Stage 1.5 force-rebuilds the pipeline image — `docker compose run --rm` reuses a cached image and does not auto-rebuild on source changes, which had masked PR #25 (harness reported "no multi-store ingestion subcommand exists" while full-sync was in the source tree). Dropped the now-unused `SMOKE_STORES` env var.
- **2026-05-30 · pipeline · dockerfile-glibc-pin** — Pinned build stage to `rust:1.95-slim-bookworm` so it shares glibc 2.36 with the `debian:bookworm-slim` runtime. The unsuffixed `rust:1.95-slim` tag had drifted onto a newer Debian base (glibc 2.39), and the binary failed at startup with `GLIBC_2.39 not found`, breaking `docker compose run --rm pipeline --help` and Stage 1.5 of `scripts/phase2_smoke.sh`.
